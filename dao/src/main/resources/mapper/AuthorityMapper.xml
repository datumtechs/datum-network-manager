<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platon.datum.admin.dao.AuthorityMapper">
    <resultMap id="BaseResultMap" type="com.platon.datum.admin.dao.entity.Authority">
        <!--@mbg.generated-->
        <!--@Table authority-->
        <id column="identity_id" jdbcType="VARCHAR" property="identityId"/>
        <result column="url" jdbcType="VARCHAR" property="url"/>
        <result column="join_time" jdbcType="TIMESTAMP" property="joinTime"/>
        <result column="is_admin" jdbcType="INTEGER" property="isAdmin"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--@mbg.generated-->
        identity_id, url, join_time, is_admin
    </sql>
    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
        <!--@mbg.generated-->
        select
        <include refid="Base_Column_List"/>
        from authority
        where identity_id = #{identityId}
    </select>

    <resultMap id="BaseResultMap_With_IdentityName" type="com.platon.datum.admin.dao.entity.Authority"
               extends="BaseResultMap">
        <result column="identity_name" property="dynamicFields.identityName" jdbcType="VARCHAR"/>
    </resultMap>

    <!--auto generated by MybatisCodeHelper on 2022-07-21-->
    <select id="selectList" resultMap="BaseResultMap_With_IdentityName">
        select a.*,
               go.node_name as identity_name
        from authority a
                 left join global_org go on a.identity_id = go.identity_id
        <where>
            <if test="keyword != null and keyword != ''">
                go.node_name like concat('%', #{keyword}, '%')
            </if>
        </where>
    </select>

    <select id="selectCount" resultType="int">
        select count(*)
        from authority a
    </select>

    <update id="refresh">
        delete
        from authority;
        <foreach collection="allAuthority" item="authority" separator=";">
            insert into authority (identity_id, url, join_time,
                                   is_admin)
            values (#{authority.identityId}, #{authority.url},
                    #{authority.joinTime},
                    #{authority.isAdmin})
        </foreach>
    </update>
</mapper>