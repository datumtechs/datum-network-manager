<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platon.datum.admin.dao.ResourceMapper">
    <resultMap id="BaseResultMap" type="com.platon.datum.admin.dao.entity.Resource">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="type" jdbcType="VARCHAR" property="type"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="value" jdbcType="INTEGER" property="value"/>
        <result column="url_resource_id" jdbcType="TIMESTAMP" property="urlResourceId"/>
        <result column="parent_id" jdbcType="TIMESTAMP" property="parentId"/>
    </resultMap>
    <sql id="Base_Column_List">
        id,
        type,
        `name`,
        `value`,
        url_resource_id,
        parent_id
    </sql>

    <select id="selectByRoleId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from resource r,
             (select * from role_resource where role_id = #{roleId}) rr
        where r.id in (rr.resource_id)
    </select>

    <select id="selectByResourceIdList" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from resource
        where id in
        <foreach collection="resourceIdList" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </select>
</mapper>